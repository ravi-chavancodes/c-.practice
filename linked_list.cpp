#include<iostream>
using namespace std;

/* // basic structure of linked list
class node{
    int data;
    node* next;

    public:
         node(int val){
            data = val;
            next = NULL;
         }
};

class list{
    node* head;
    node* tail;

    public:
    list(){
        head=NULL;
        tail=NULL;
    }
    void pushfront(int val) {
         Node* newNode = new Node(val); //dynamicz

        if(head==NULL)
        {
         head=tail=newNode
         }
         else{
              newNode->next = head
          }
              }
};
int main(){
    list ll();
    ll.push_front(3);
    ll.push_front(2);
    ll.push_front(1);
    return 0;
}
    */

    // 